<%= simple_form_for(@cost) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :estimated_cost %>
    <%= f.association :from_distrit %>
    <%= f.association :to_distrit %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
